REFERENCE_WINDOW_START=2026-05-01T01:00:00
REFERENCE_WINDOW_END=2026-05-01T02:00:00

# from training/artifacts/model_versions.json after monitoring/main.py
CHAMPION_VERSION=
CHALLENGER_VERSION=

# Shared by router, both serving containers, and the canary evaluator.
# Required; docker-compose will refuse to start if unset. Generate with:
#   openssl rand -hex 32
ADMIN_TOKEN=